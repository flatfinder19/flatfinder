// import {
// } from '../actions';

const initialState = {
  data: 'testtttt',
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};

